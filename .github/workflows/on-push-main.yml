name: Deploy to Amazon S3 and CloudFront

on:
  push:
    branches: ['main']

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/deploy-to-aws.yml
